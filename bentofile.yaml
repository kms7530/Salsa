name: "vigilant"

services:
  DINO: 
    scaling:
      max_replicas: 2
      min_replicas: 1
    config_overrides:
      traffic:
        timeout: 10
        
  VisionLanguage:
    scaling:
      max_replicas: 2
      min_replicas: 1
    config_overrides:
      traffic:
        timeout: 30